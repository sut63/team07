// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
	"github.com/team07/app/ent/carservice"
	"github.com/team07/app/ent/distance"
)

// DistanceCreate is the builder for creating a Distance entity.
type DistanceCreate struct {
	config
	mutation *DistanceMutation
	hooks    []Hook
}

// SetDistance sets the Distance field.
func (dc *DistanceCreate) SetDistance(s string) *DistanceCreate {
	dc.mutation.SetDistance(s)
	return dc
}

// AddDisidIDs adds the disid edge to Carservice by ids.
func (dc *DistanceCreate) AddDisidIDs(ids ...int) *DistanceCreate {
	dc.mutation.AddDisidIDs(ids...)
	return dc
}

// AddDisid adds the disid edges to Carservice.
func (dc *DistanceCreate) AddDisid(c ...*Carservice) *DistanceCreate {
	ids := make([]int, len(c))
	for i := range c {
		ids[i] = c[i].ID
	}
	return dc.AddDisidIDs(ids...)
}

// Mutation returns the DistanceMutation object of the builder.
func (dc *DistanceCreate) Mutation() *DistanceMutation {
	return dc.mutation
}

// Save creates the Distance in the database.
func (dc *DistanceCreate) Save(ctx context.Context) (*Distance, error) {
	if _, ok := dc.mutation.Distance(); !ok {
		return nil, &ValidationError{Name: "Distance", err: errors.New("ent: missing required field \"Distance\"")}
	}
	if v, ok := dc.mutation.Distance(); ok {
		if err := distance.DistanceValidator(v); err != nil {
			return nil, &ValidationError{Name: "Distance", err: fmt.Errorf("ent: validator failed for field \"Distance\": %w", err)}
		}
	}
	var (
		err  error
		node *Distance
	)
	if len(dc.hooks) == 0 {
		node, err = dc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*DistanceMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			dc.mutation = mutation
			node, err = dc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(dc.hooks) - 1; i >= 0; i-- {
			mut = dc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, dc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (dc *DistanceCreate) SaveX(ctx context.Context) *Distance {
	v, err := dc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (dc *DistanceCreate) sqlSave(ctx context.Context) (*Distance, error) {
	d, _spec := dc.createSpec()
	if err := sqlgraph.CreateNode(ctx, dc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	d.ID = int(id)
	return d, nil
}

func (dc *DistanceCreate) createSpec() (*Distance, *sqlgraph.CreateSpec) {
	var (
		d     = &Distance{config: dc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: distance.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: distance.FieldID,
			},
		}
	)
	if value, ok := dc.mutation.Distance(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: distance.FieldDistance,
		})
		d.Distance = value
	}
	if nodes := dc.mutation.DisidIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   distance.DisidTable,
			Columns: []string{distance.DisidColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: carservice.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return d, _spec
}
