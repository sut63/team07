// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team07/app/ent/distances"
)

// Distances is the model entity for the Distances schema.
type Distances struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Distances holds the value of the "Distances" field.
	Distances string `json:"Distances,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DistancesQuery when eager-loading is set.
	Edges DistancesEdges `json:"edges"`
}

// DistancesEdges holds the relations/edges for other nodes in the graph.
type DistancesEdges struct {
	// Disid holds the value of the disid edge.
	Disid []*Carservice
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DisidOrErr returns the Disid value or an error if the edge
// was not loaded in eager-loading.
func (e DistancesEdges) DisidOrErr() ([]*Carservice, error) {
	if e.loadedTypes[0] {
		return e.Disid, nil
	}
	return nil, &NotLoadedError{edge: "disid"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Distances) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // Distances
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Distances fields.
func (d *Distances) assignValues(values ...interface{}) error {
	if m, n := len(values), len(distances.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	d.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field Distances", values[0])
	} else if value.Valid {
		d.Distances = value.String
	}
	return nil
}

// QueryDisid queries the disid edge of the Distances.
func (d *Distances) QueryDisid() *CarserviceQuery {
	return (&DistancesClient{config: d.config}).QueryDisid(d)
}

// Update returns a builder for updating this Distances.
// Note that, you need to call Distances.Unwrap() before calling this method, if this Distances
// was returned from a transaction, and the transaction was committed or rolled back.
func (d *Distances) Update() *DistancesUpdateOne {
	return (&DistancesClient{config: d.config}).UpdateOne(d)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (d *Distances) Unwrap() *Distances {
	tx, ok := d.config.driver.(*txDriver)
	if !ok {
		panic("ent: Distances is not a transactional entity")
	}
	d.config.driver = tx.drv
	return d
}

// String implements the fmt.Stringer.
func (d *Distances) String() string {
	var builder strings.Builder
	builder.WriteString("Distances(")
	builder.WriteString(fmt.Sprintf("id=%v", d.ID))
	builder.WriteString(", Distances=")
	builder.WriteString(d.Distances)
	builder.WriteByte(')')
	return builder.String()
}

// DistancesSlice is a parsable slice of Distances.
type DistancesSlice []*Distances

func (d DistancesSlice) config(cfg config) {
	for _i := range d {
		d[_i].config = cfg
	}
}
