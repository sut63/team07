// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team07/app/ent/hospital"
)

// Hospital is the model entity for the Hospital schema.
type Hospital struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Hospital holds the value of the "hospital" field.
	Hospital string `json:"hospital,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the HospitalQuery when eager-loading is set.
	Edges HospitalEdges `json:"edges"`
}

// HospitalEdges holds the relations/edges for other nodes in the graph.
type HospitalEdges struct {
	// Receive holds the value of the receive edge.
	Receive []*Transport
	// Send holds the value of the send edge.
	Send []*Transport
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ReceiveOrErr returns the Receive value or an error if the edge
// was not loaded in eager-loading.
func (e HospitalEdges) ReceiveOrErr() ([]*Transport, error) {
	if e.loadedTypes[0] {
		return e.Receive, nil
	}
	return nil, &NotLoadedError{edge: "receive"}
}

// SendOrErr returns the Send value or an error if the edge
// was not loaded in eager-loading.
func (e HospitalEdges) SendOrErr() ([]*Transport, error) {
	if e.loadedTypes[1] {
		return e.Send, nil
	}
	return nil, &NotLoadedError{edge: "send"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Hospital) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // hospital
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Hospital fields.
func (h *Hospital) assignValues(values ...interface{}) error {
	if m, n := len(values), len(hospital.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	h.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field hospital", values[0])
	} else if value.Valid {
		h.Hospital = value.String
	}
	return nil
}

// QueryReceive queries the receive edge of the Hospital.
func (h *Hospital) QueryReceive() *TransportQuery {
	return (&HospitalClient{config: h.config}).QueryReceive(h)
}

// QuerySend queries the send edge of the Hospital.
func (h *Hospital) QuerySend() *TransportQuery {
	return (&HospitalClient{config: h.config}).QuerySend(h)
}

// Update returns a builder for updating this Hospital.
// Note that, you need to call Hospital.Unwrap() before calling this method, if this Hospital
// was returned from a transaction, and the transaction was committed or rolled back.
func (h *Hospital) Update() *HospitalUpdateOne {
	return (&HospitalClient{config: h.config}).UpdateOne(h)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (h *Hospital) Unwrap() *Hospital {
	tx, ok := h.config.driver.(*txDriver)
	if !ok {
		panic("ent: Hospital is not a transactional entity")
	}
	h.config.driver = tx.drv
	return h
}

// String implements the fmt.Stringer.
func (h *Hospital) String() string {
	var builder strings.Builder
	builder.WriteString("Hospital(")
	builder.WriteString(fmt.Sprintf("id=%v", h.ID))
	builder.WriteString(", hospital=")
	builder.WriteString(h.Hospital)
	builder.WriteByte(')')
	return builder.String()
}

// Hospitals is a parsable slice of Hospital.
type Hospitals []*Hospital

func (h Hospitals) config(cfg config) {
	for _i := range h {
		h[_i].config = cfg
	}
}
