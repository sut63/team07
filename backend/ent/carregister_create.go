// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
	"github.com/team07/app/ent/carregister"
)

// CarregisterCreate is the builder for creating a Carregister entity.
type CarregisterCreate struct {
	config
	mutation *CarregisterMutation
	hooks    []Hook
}

// Mutation returns the CarregisterMutation object of the builder.
func (cc *CarregisterCreate) Mutation() *CarregisterMutation {
	return cc.mutation
}

// Save creates the Carregister in the database.
func (cc *CarregisterCreate) Save(ctx context.Context) (*Carregister, error) {
	var (
		err  error
		node *Carregister
	)
	if len(cc.hooks) == 0 {
		node, err = cc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CarregisterMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			cc.mutation = mutation
			node, err = cc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(cc.hooks) - 1; i >= 0; i-- {
			mut = cc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, cc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (cc *CarregisterCreate) SaveX(ctx context.Context) *Carregister {
	v, err := cc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (cc *CarregisterCreate) sqlSave(ctx context.Context) (*Carregister, error) {
	c, _spec := cc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	c.ID = int(id)
	return c, nil
}

func (cc *CarregisterCreate) createSpec() (*Carregister, *sqlgraph.CreateSpec) {
	var (
		c     = &Carregister{config: cc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: carregister.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: carregister.FieldID,
			},
		}
	)
	return c, _spec
}
